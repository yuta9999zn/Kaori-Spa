spring:
  application:
    name: inventory-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/kaori_inventory}
    username: ${DB_USER:kaori}
    password: ${DB_PASS:kaori}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    baseline-on-migrate: true
    schemas: inventory
server:
  port: ${PORT:8089}
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
